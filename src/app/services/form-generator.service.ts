import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root'
})
export class FormGeneratorService {

  // _generatedFormComponentCode = computed(() => {
  //   fullComponentCode2(this._formCanvas());
  // });

  // generateFormComponentCode() {
  //   this._generatedFormComponentCode.set(fullComponentCode2(this._formCanvas()));
  // }

  // set formCanvas(value: FormElementField[][]) {
  //   this._formCanvas.set(value);
  // }

  // get formCanvas() {
  //   return this._formCanvas();
  // }



  // take formCanvas;

  // generate a form string

  // return a form string;

  // make the form string accsible;






}
